name: Complex

on: push

jobs:
ubuntu:
  runs-on: ubuntu-latest
  steps:
  - run:date
windows:
  runs-on: window-latest
  steps:
  - run:date
macos:
  runs-on:macoS-latest
  steps:
  - run:date
depends:
needs:[ubunntu, windows, macos]
runs-on:macoS-latest
steps:
- run:date
